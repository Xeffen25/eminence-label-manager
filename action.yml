name: Eminence Label Manager
description:
  A GitHub Action that manages a repository's labels using a JSON file.
author: Xeffen25

# Add your action's branding here. This will appear on the GitHub Marketplace.
branding:
  icon: tag
  color: red

# Define your inputs here.
inputs:
  token:
    description: Your GITHUB_TOKEN
    required: true

runs:
  using: node20
  main: dist/index.js
